import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-swamphacks-workshop',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
